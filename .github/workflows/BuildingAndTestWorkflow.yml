name: BuildingAndTestWorkflow.yml
on:
  pull_request:
    branches:
      - main


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: ./.github/actions/setup-java-project
      - name:
        run: mvn clean test
